{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "shell",
			"command": "docker start trophy-db && cargo run",
			"problemMatcher": ["$rustc"],
			"group": {
				"kind": "build",
				"isDefault": true
			},
			"options": {
				"cwd": "${workspaceFolder}/be"
			},
			"label": "rust: cargo build"
		}
	]
}
